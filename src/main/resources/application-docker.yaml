spring:
  datasource:
    url: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:vehicle_db}?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${MYSQL_USER:rick}
    password: ${MYSQL_PASSWORD:change_me_in_production}
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis
      port: 6379

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}

jwt:
  secret: ${JWT_SECRET:c2VjcmV0LWtleS1mb3ItdmVoaWNsZS1tYW5hZ2VtZW50LXN5c3RlbS0yMDI2LWp3dC10b2tlbg==}
  expiration: 86400000
